#include <stdio.h>

int main(int argc, char* argv[]) {
    char* name = "Tyler Luke";
    printf("The memory address of name is: %p\n", name);
    printf("The value stored in name is: %s", name);
    return 0;
}